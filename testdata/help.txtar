# help for --help
exec parquetry --help
! stderr .
cmp stdout help

# help for -h
exec parquetry -h
! stderr .
cmp stdout help

# help for cat
exec parquetry cat --help
! stderr .
cmp stdout help.cat

# help for to
exec parquetry to --help
! stderr .
cmp stdout help.to

# help for to csv
exec parquetry to csv --help
! stderr .
cmp stdout help.to.csv
-- help --
Usage: parquetry <command> [flags]

Tooling for parquet files

Flags:
  -h, --help    Show context-sensitive help.

Commands:
  cat         print a parquet file
  schema      print a parquet schema
  to csv      convert parquet to CSV
  to json     convert parquet to JSON
  to jsonl    convert parquet to JSON-Lines
  where       filter a parquet file

Run "parquetry <command> --help" for more information on a command.
-- help.cat --
Usage: parquetry cat <file> ... [flags]

print a parquet file

Arguments:
  <file> ...    parqet files to print

Flags:
  -h, --help           Show context-sensitive help.

      --head=INT-64    include first +n or skip first -n records
      --tail=INT-64    include last +n or skip last -n records

Output Format:
  --go       format as go (default)
  --csv      format as CSV
  --json     format as JSON
  --jsonl    format as JSON-Lines
-- help.to --
Usage: parquetry to <command> [flags]

convert parquet to...

Flags:
  -h, --help    Show context-sensitive help.

Commands:
  to csv      convert parquet to CSV
  to json     convert parquet to JSON
  to jsonl    convert parquet to JSON-Lines
-- help.to.csv --
Usage: parquetry to csv <file> ... [flags]

convert parquet to CSV

Arguments:
  <file> ...    parqet files to print

Flags:
  -h, --help           Show context-sensitive help.

      --head=INT-64    include first +n or skip first -n records
      --tail=INT-64    include last +n or skip last -n records
